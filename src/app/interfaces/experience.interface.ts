export interface Stat {
  id: number;
  value: string;
  suffix?: string;
  label: string;
  description: string;
}

export interface Feature {
  icon: string;
  title: string;
  description: string;
}
